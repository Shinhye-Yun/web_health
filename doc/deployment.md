## Build a container image using CI pipeline and ship the image to AWS ECR
### How to optimize build image?

## Deploy an EKS cluster - build an AMI with packer

## Install Flux in EKS cluster to reconcile helm release
